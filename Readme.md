

# Agentic RAG chatbot that is made with FastAPI


It chunks and embeds the documents in the uploads folder.



### Next Step:
1. Fix the `retrieve_and_answer()` function and make it work. 
2. Then create neat UI (for now, make it similar to the [Agent Chat UI](https://agentchat.vercel.app/?apiUrl=http://localhost:2024&assistantId=agent))
